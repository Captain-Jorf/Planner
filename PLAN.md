# برنامه‌ریز روزانه — سند طراحی و درخت تفکر

> Persian Daily Planner — Design doc & Tree‑of‑Thought

## هدف
یک اپلیکیشن **برنامه‌ریزی روزانه فارسی** با تاریخ **شمسی**، طراحی
**سه‌بعدی**، تم رنگی مینیمال پست‌مدرن اما «rich»، و خروجی نهایی **APK**.

---

## درخت تفکر (Tree of Thought)

### شاخه ۱ — بستر فنی (چطور به APK برسیم؟)
- **الف) Flutter/Kotlin بومی** → نیاز به Android SDK + Gradle + Java.
  در این سندباکس `dl.google.com`, `services.gradle.org` و مخازن Maven
  مسدود‌اند و SDK نصب نیست. ❌ داخل سندباکس ساخته نمی‌شود.
- **ب) Capacitor (وب + WebView بومی)** → کد UI با وب ساخته می‌شود، Capacitor
  آن را در یک پروژه‌ی اندروید بسته‌بندی می‌کند. بیلد نهایی APK باز هم به
  Android SDK نیاز دارد. ✅ **انتخاب شد** — چون:
  1. پیش‌نمایش زنده‌ی فوری در مرورگر کاربر ممکن است (Vite dev server).
  2. ساخت APK را به **GitHub Actions** می‌سپاریم (اینترنت + SDK کامل دارد).
- **نتیجه:** Vite + Vanilla JS (سبک، بدون فریم‌ورک سنگین) + Capacitor +
  ورک‌فلوی GitHub Actions که در هر push یک `app-debug.apk` می‌سازد.

### شاخه ۲ — معماری UI
- تک‌صفحه‌ای (SPA) با روتر ساده‌ی هش‌محور.
- ۵ نما مطابق ۵ دکمه‌ی نوار پایین.
- حالت (state) در `localStorage` ذخیره می‌شود → آفلاین و ماندگار.

### شاخه ۳ — نوار پایین (۵ دکمه، home وسط)
```
[ تقویم ]  [ وظایف ]  (( خانه ))  [ عادت‌ها ]  [ تنظیمات ]
   ۱          ۲          مرکز         ۴            ۵
```
- دکمه‌ی «خانه» برجسته، دایره‌ای و بالاتر از نوار (شناور، سه‌بعدی).

### شاخه ۴ — سه‌بعدی بودن
- عمق واقعی با `transform: perspective()` + `rotateX/Y` روی کارت‌ها.
- سایه‌های لایه‌ای چندگانه (long shadow) برای حس ارتفاع.
- افکت tilt کارت‌ها هنگام لمس/هاور.
- دکمه‌ی خانه با گرادیان و سایه‌ی برجسته → حس شناور بودن.

### شاخه ۵ — تم رنگی (۲۰ تا ۵۰ رنگ)
- پالت ۳۲ رنگی: ۷ خنثی + ۱۳ اکسنت شاد + ۴ معنایی + گرادیان‌ها.
- کنتراست: متن تیره روی پس‌زمینه‌ی روشن (نسبت > 4.5:1)، اکسنت‌ها فقط
  برای برجسته‌سازی. پس‌زمینه و دکمه‌ها ساده؛ تصاویر illustration پرجزئیات.
- دو حالت روشن/تیره با کلید تغییر تم.

### شاخه ۶ — تصویرسازی وکتور (SVG)
- همه‌ی آیکون‌ها و illustrationها SVG درون‌خطی (بدون فایل باینری).
- illustrationهای اختصاصی: قهرمان صفحه‌ی خانه، حالت خالی، جشن اتمام.

---

## پالت رنگ (۳۲ رنگ)
خنثی: ink, slate, gray, mist, cloud, paper, white
اکسنت: coral, tangerine, sunflower, lime, mint, teal, sky, azure,
indigo, violet, magenta, rose, blush
معنایی: success, warning, danger, info
(+ نسخه‌های روشن/تیره‌ی اکسنت‌ها در گرادیان‌ها)

## پنج نما
1. **تقویم** — نمای ماه شمسی + رویدادهای روز.
2. **وظایف** — فهرست کارها با اولویت و **زمان**، افزودن/حذف/تکمیل.
3. **خانه** — داشبورد امروز: تاریخ شمسی، حلقه‌ی پیشرفت، **تایم‌لاین کوچک** و آمار.
4. **برنامه (Agenda)** — تایم‌لاین کامل روز، دسته‌بندی کارها به صبح/بعدازظهر/شب.
5. **تنظیمات** — تغییر تم روشن/تیره، نام کاربر، پاک‌سازی داده.

## به‌روزرسانی نسخه ۲
- **رنگ‌ها کاملاً سالید و ریچ** شدند (حذف تمام گرادیان‌ها)؛ عمق سه‌بعدی با
  سایه‌های سالید (offset shadow) بازسازی شد.
- **رندر بهینه**: شل اپ فقط یک‌بار ساخته می‌شود و فقط محتوای نمای فعال
  به‌روزرسانی می‌شود (`refresh()` درجا، بدون بازساخت DOM و بدون پرش اسکرول).
- بخش «عادت‌ها» به **«برنامه/تایم‌لاین»** مبتنی بر وظایف تبدیل شد.
- **تصاویر illustration** با هوش مصنوعی تولید و به‌صورت PNG شفاف و فشرده
  (هرکدام ~۸۵ کیلوبایت) در هیرو و حالت‌های خالی/جشن تعبیه شدند.
- آیکون‌ها و اجزای تصویری بیشتر **وکتور (SVG)** شدند.

---

## راند نهایی — رساندن به سطح Taskito (نسخه ۷)

درخت‌تصمیم برای هر ویژگی:

1. **مدت‌زمان + تایم‌بلاکینگ کارها**
   - گزینه‌ها: (الف) فیلد پایان جدا (ب) فیلد مدت‌زمان دقیقه‌ای.
   - انتخاب: مدت‌زمان (`dur` دقیقه) + محاسبه‌ی پایان از شروع. ساده‌تر برای ورودی، و بلوک زمانی در برنامه رسم می‌شود.

2. **تگ/دسته‌ی رنگی**
   - گزینه‌ها: (الف) تگ آزاد متنی (ب) مجموعه‌ی تگ‌های از پیش با رنگ.
   - انتخاب: مجموعه‌ی قابل‌مدیریت `tags:[{id,name,color}]`؛ هر کار `tags:[id...]`. ساخت/حذف تگ در تنظیمات.

3. **کارهای تکرارشونده (بازگشت عادت‌ها)**
   - گزینه‌ها: (الف) ماده‌سازی نمونه‌ها (ب) قاعده + `doneDays`.
   - انتخاب: قاعده `repeat: none|daily|weekly|monthly` نسبت به `day` لنگر + آرایه‌ی `doneDays` برای وضعیت انجام هر روز. بدون تکثیر داده.

4. **یادداشت‌ها**
   - `notes:[{id,text,color,ts}]` — صفحه‌ی سبک با کارت‌های رنگی. دسترسی از خانه/تنظیمات.

5. **تایم‌لاین یکپارچه**
   - ادغام کارها (با بلوک زمانی) و رویدادهای تقویم در یک جریان زمانی واحد در صفحه‌ی «برنامه».

6. **قالب‌ها (Templates)**
   - `templates:[{id,name,items:[...]}]` — اعمال با یک ضربه چند کار را به امروز اضافه می‌کند.

کلید ذخیره‌سازی → `planner.state.v4` (طرح‌واره‌ی جدید).
